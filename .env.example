# Add a PostgreSQL connection string when the PostGIS database is introduced.
# Never commit the real value.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wnyatlas

# Public application URL used for metadata and canonical links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
